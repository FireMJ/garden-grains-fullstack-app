{
  "buildCommand": "rm -rf .next node_modules && npm install --legacy-peer-deps && next build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "env": {
    "NEXT_TYPESCRIPT_IGNORE_BUILD_ERRORS": "1"
  }
}
